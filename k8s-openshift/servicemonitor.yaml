apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fastapi-metrics
  labels:
    app: fastapi-metrics
    release: monitoring
spec:
  selector:
    matchLabels:
      app: fastapi-metrics
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
